const { Fragment } = require("react");

const StartWindow = () => {
  return (
    <Fragment>
      <div className="bg-base-base21 h-full">Welcome Page!</div>
    </Fragment>
  );
};

export default StartWindow;
